<Modal::Default @modalIsOpened={{@modalIsOpened}} @options={{@options}} @confirm={{@onConfirm}} @decline={{@onDecline}}>
    <div class="modal-body-container space-y-4">

        <ContentPanel @title="Thông tin lệnh chuyển" @open={{true}} @pad={{true}} @panelBodyClass="bg-white dark:bg-gray-800">
            <div class="grid grid-cols-2 gap-3 text-xs">
                <div>
                    <div class="text-gray-400">Xe chuyển</div>
                    <div class="text-sm font-medium text-white">{{this.transfer.from_vehicle_name}}</div>
                </div>
                <div>
                    <div class="text-gray-400">Xe nhận</div>
                    <div class="text-sm font-medium text-white">{{this.transfer.to_vehicle_name}}</div>
                </div>
            </div>
            <div class="mt-3 text-xs text-yellow-400">Lệnh chuyển sẽ không được tính vào tiền tồn của các xe.</div>
        </ContentPanel>

        <ContentPanel @title="Lý do từ chối" @open={{true}} @pad={{true}} @panelBodyClass="bg-white dark:bg-gray-800">
            <InputGroup @name="Lý do từ chối (bắt buộc)">
                <Textarea
                    @value={{this.rejectNote}}
                    class="form-input w-full text-xs"
                    {{on "input" this.updateRejectNote}}
                    placeholder="Nhập lý do từ chối để lái xe được biết..."
                    rows={{3}}
                />
            </InputGroup>
        </ContentPanel>

    </div>
</Modal::Default>
